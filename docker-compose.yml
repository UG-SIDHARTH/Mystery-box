version: '3.8'

services:
  web:
    build: .
    ports:
      - "80:80"
    volumes:
      - ./app/uploads:/app/app/uploads
      - ./data:/app/data
    environment:
      - DATABASE_URL=sqlite+aiosqlite:///./data/event_voting.db
    env_file:
      - .env
    restart: unless-stopped
